# Real-Time-Collaborative-Document-Editor
Co-Edit is a full-stack web app allowing multiple users to edit documents simultaneously. Built with Node.js and Socket.io, it uses WebSockets for instant data syncing and conflict resolution. Features include rich-text editing via Quill.js, persistent MongoDB storage, and a live presence system to track active collaborators.
